# Auto detect text files and perform LF normalization
* text=auto

# Enforce LF for shell scripts
*.sh text eol=lf

# Enforce CRLF for PowerShell and batch scripts
*.ps1 text eol=crlf
*.bat text eol=crlf
